# Placeholder Dockerfile — full multi-stage build comes in Fáze 4.
FROM eclipse-temurin:21-jre

WORKDIR /app

# TODO (fáze 4): multi-stage build (Gradle build stage + slim runtime stage),
# copy built jar, expose port, set entrypoint.
