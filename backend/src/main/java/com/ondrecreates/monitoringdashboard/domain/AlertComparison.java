package com.ondrecreates.monitoringdashboard.domain;

/** Comparison an {@link Alert} uses to decide whether a metric value is out of bounds. */
public enum AlertComparison {
    GREATER_THAN,
    LESS_THAN
}
